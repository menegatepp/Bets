[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt && playwright install chromium --with-deps"
]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port 8080"
